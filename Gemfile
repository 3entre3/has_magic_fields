# frozen_string_literal: true

source "https://rubygems.org"

gemspec

gem "rails", " >= 4.0.0"

group :test do
  gem "byebug"
  gem "rspec-sqlimit"
  gem "rspec-rails", " >= 3.0.0"
  gem "sqlite3"
  gem "database_cleaner"
end
